# Task Graph Edge Law

L5 may publish task-facing signals upward only as bounded status or metric classes.
L5 never owns a studio task graph, build graph, or editor transaction graph.

Any upper graph edge that depends on L5 must terminate on typed bridge outputs, not on hidden bridge state.
