# engine_session_handles Level

Canonical layer: `engine_session_handles`

Exists to own exactly one bridge role.
Core operational meaning: opaque session tokens.
It explicitly does not own: refs, mutation authority.
