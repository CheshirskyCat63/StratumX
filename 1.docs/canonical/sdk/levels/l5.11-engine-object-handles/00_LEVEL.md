# engine_object_handles Level

Canonical layer: `engine_object_handles`

Exists to own exactly one bridge role.
Core operational meaning: opaque object tokens.
It explicitly does not own: refs, mutation authority.
