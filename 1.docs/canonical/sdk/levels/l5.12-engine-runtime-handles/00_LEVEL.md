# engine_runtime_handles Level

Canonical layer: `engine_runtime_handles`

Exists to own exactly one bridge role.
Core operational meaning: opaque runtime service tokens.
It explicitly does not own: refs, mutation authority.
