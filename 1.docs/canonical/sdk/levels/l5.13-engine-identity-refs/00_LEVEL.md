# engine_identity_refs Level

Canonical layer: `engine_identity_refs`

Exists to own exactly one bridge role.
Core operational meaning: immutable identity projections.
It explicitly does not own: handles, mutation.
