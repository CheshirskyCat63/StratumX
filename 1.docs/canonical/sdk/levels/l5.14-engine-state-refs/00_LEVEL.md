# engine_state_refs Level

Canonical layer: `engine_state_refs`

Exists to own exactly one bridge role.
Core operational meaning: immutable or monotonic state projections.
It explicitly does not own: handles, mutation.
