# engine_artifact_refs Level

Canonical layer: `engine_artifact_refs`

Exists to own exactly one bridge role.
Core operational meaning: immutable deterministic artifact projections.
It explicitly does not own: state refs, build ownership.
