# compat_versions Level

Canonical layer: `compat_versions`

Exists to own exactly one bridge role.
Core operational meaning: immutable compatibility version facts.
It explicitly does not own: verdict, handle, ref.
