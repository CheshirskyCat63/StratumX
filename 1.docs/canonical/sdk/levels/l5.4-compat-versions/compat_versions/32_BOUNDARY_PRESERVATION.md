# Boundary Preservation

## Adjacent above
verdicts, gates, and boundary readers

## Adjacent below
shared registry ids and package stack version only

## This layer must not become
- an adjacent semantic class
- editor orchestration
- engine execution logic
- payload-body ownership outside its local contract

## Why the boundary matters
`compat_versions` would lose semantic sharpness if it collapsed into an adjacent layer.
