# compat_capabilities Level

Canonical layer: `compat_capabilities`

Exists to own exactly one bridge role.
Core operational meaning: immutable capability facts.
It explicitly does not own: verdict, handle, ref.
