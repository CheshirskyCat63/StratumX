# compat_profiles Level

Canonical layer: `compat_profiles`

Exists to own exactly one bridge role.
Core operational meaning: immutable profile and cost facts.
It explicitly does not own: verdict, handle, ref.
