# compat_verdicts Level

Canonical layer: `compat_verdicts`

Exists to own exactly one bridge role.
Core operational meaning: derived compatibility decisions.
It explicitly does not own: facts, handles, refs.
