# StratumX L6A Assistant Runtime Constitution

`L6A` owns assistant runtime mediation only.
It may package evidence, request models, stage proposals, lower work into legal `L6` commands, run safety gates, and apply or revert only through legal `L6` transactions.
It never owns editor truth, plan truth, or direct engine access.
