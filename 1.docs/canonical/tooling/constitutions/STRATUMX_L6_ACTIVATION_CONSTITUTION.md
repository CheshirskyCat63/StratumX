# StratumX L6 Activation Constitution

`L6` is the only always-hot owner in the upper stack.
Direct editor actions must not wake `L7` or `L7A` unless explicit planning or orchestration is requested.
Passive reads do not create hidden activation.
