# StratumX L7 Studio Orchestration Constitution

`L7` owns cold studio orchestration only.
It compiles campaigns, governance policies, automation requests, and reporting work.
It never becomes frame-level runtime and never owns editor mutation authority or direct engine writes.
