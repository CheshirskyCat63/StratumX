# StratumX Memory GPU Disk Constitution

Every upper-stack runtime must expose bounded memory, bounded queueing, bounded GPU residency, and bounded disk growth.
Caches are evictable, artifacts are manifest-backed, previews are budgeted, and reasoning traces are retained only under explicit policy.
Silent growth is unconstitutional.
