# tool_selection

Role class: fact

Canonical role:
own current selection set.

One data kind law:
This layer owns only selection facts.

Minimal operational meaning:
publishes current selection facts

Forbidden drift:
inspection rendering, previewing, session ownership, scene mutation.
