# tool_release_intents

Role class: intent

Canonical role:
express build, package, export, and test requests.

One data kind law:
This layer owns only release workflow intents.

Minimal operational meaning:
publishes release intents only

Forbidden drift:
build execution, package execution, release truth, deploy runtime.
