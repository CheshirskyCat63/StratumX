# tool_assistant_intents

Role class: intent

Canonical role:
express assistant-originated workflow requests.

One data kind law:
This layer owns only assistant workflow intents.

Minimal operational meaning:
publishes assistant intents only

Forbidden drift:
generation execution, direct project mutation, direct engine access, hidden planning state.
