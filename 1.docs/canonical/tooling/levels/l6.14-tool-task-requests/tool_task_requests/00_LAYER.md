# tool_task_requests

Role class: request

Canonical role:
describe background units of work.

One data kind law:
This layer owns only task request descriptors.

Minimal operational meaning:
publishes task requests only

Forbidden drift:
task execution runtime, task result ownership, session truth.
