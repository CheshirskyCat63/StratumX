# tool_focus_refs

Role class: ref

Canonical role:
own current UI and object focus references.

One data kind law:
This layer owns only focus refs.

Minimal operational meaning:
publishes current focus refs only

Forbidden drift:
selection truth, panel layout, rendering, activation rules.
